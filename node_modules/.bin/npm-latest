#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../npm-latest/bin/npm-latest" "$@"
  ret=$?
else 
  node  "$basedir/../npm-latest/bin/npm-latest" "$@"
  ret=$?
fi
exit $ret
